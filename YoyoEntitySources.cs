using Terraria.ModLoader;
using Terraria.ID;
using Terraria;
using Terraria.DataStructures;

namespace CombinationsMod
{
    public class YoyoClipClass : EntitySource_Parent
    {
        public YoyoClipClass(Entity entity, string context = null) : base(entity, context)
        {
            // This class makes a new spawn source for the yoyo projectiles to inherit so they aren't detected as regular yoyos.
        }
    }
    public class YoyoClipClassFire : EntitySource_Parent
    {
        public YoyoClipClassFire(Entity entity, string context = null) : base(entity, context)
        {
            // This class makes a new spawn source for the yoyo projectiles to inherit so they aren't detected as regular yoyos.
        }
    }
    public class YoyoClipClassEarth : EntitySource_Parent
    {
        public YoyoClipClassEarth(Entity entity, string context = null) : base(entity, context)
        {
            // This class makes a new spawn source for the yoyo projectiles to inherit so they aren't detected as regular yoyos.
        }
    }
    public class YoyoClipClassWater : EntitySource_Parent
    {
        public YoyoClipClassWater(Entity entity, string context = null) : base(entity, context)
        {
            // This class makes a new spawn source for the yoyo projectiles to inherit so they aren't detected as regular yoyos.
        }
    }
    public class YoyoClipClassElectricity : EntitySource_Parent
    {
        public YoyoClipClassElectricity(Entity entity, string context = null) : base(entity, context)
        {
            // This class makes a new spawn source for the yoyo projectiles to inherit so they aren't detected as regular yoyos.
        }
    }

    public class YoyoClipClassNormal : EntitySource_Parent
    {
        public YoyoClipClassNormal(Entity entity, string context = null) : base(entity, context)
        {
            // This class makes a new spawn source for the yoyo projectiles to inherit so they aren't detected as regular yoyos.
        }
    }
}
